# CLAUDE.md - RustBebyUI プロジェクトガイド

## プロジェクト概要
Bevy 0.15 を使ったボクセルワールド探索型アドベンチャーゲーム。
VSCode風マルチウィンドウUIで情報を集め、島の因習の謎を解く。
ウィンドウタイトル: "One O Net - Cyber Studio v0.9"

## 技術スタック
- **言語:** Rust (Edition 2024)
- **フレームワーク:** Bevy 0.15 (ECS アーキテクチャ)
- **主要クレート:** futures-lite (非同期チャンク処理)

## ビルド・実行
```bash
cargo build          # ビルド
cargo run            # 実行
cargo check          # 型チェック
cargo clippy         # Lint
cargo fmt            # フォーマット
```

## ソースコード構成 (src/)

### ドメイン層 (型定義・データ)
- `ボクセル世界.rs` — ボクセル型・チャンク構造体・定数
- `四面体世界.rs` — 四面体チャンク構造体・分割定義・隣接テーブル
- `地形生成.rs` — 共通の地形ノイズ関数と両チャンク型の地形生成

### 描画層 (メッシュ・チャンク管理)
- `メッシュ生成.rs` — 立方体メッシュの LOD 対応生成
- `四面体メッシュ生成.rs` — 四面体メッシュ生成 (面カリング付き)
- `チャンク管理.rs` — 立方体チャンクのストリーミング・LOD管理
- `四面体チャンク管理.rs` — 四面体チャンクのストリーミング

### インタラクション層 (カメラ・物理)
- `カメラ制御.rs` — 飛行/歩行モード・水中物理
- `衝突判定.rs` — 7点チェックによる衝突判定

### UI・シーン層
- `シーン初期化.rs` — カメラ・ライト・フォグ・マテリアル初期化
- `UI設定.rs` — サイバーテーマ UI (レイアウト構築・イベント処理)

### ストーリーエンジン層
- `ストーリーエンジン.rs` — 宣言的イベント定義・時間管理・フラグ/ドキュメント/メッセージ/カレンダーストア
- `ストーリー評価.rs` — Bevyシステム関数群 (評価ループ・効果適用・操作・UI表示更新・天候ビジュアル)
- `サンプルストーリー.rs` — ストーリーコンテンツ (全3章・エンディング3分岐)

### エントリポイント
- `main.rs` — モジュール宣言・プラグイン登録・システム登録

## アーキテクチャ方針
- 仕様書駆動開発 (SDD): `_doc/` 配下に仕様・設計ドキュメント
- Bevy ECS パターン: Component 定義 → System 実装
- 二重レンダリング: 立方体ボクセルと四面体ボクセルをタブで切替
- 宣言的ストーリーエンジン: イベントをデータとして定義し、エンジンが条件評価→効果適用
- VSCode風マルチウィンドウ: Calendar, Messages, Documents 等のタブで情報閲覧
- 責務分離: ドメイン → 描画 → ストーリー → インタラクション → UI の層構造
- 将来: 組み込みHTTPサーバーでブラウザ連携 (偽サイト探索)

## コーディング規約
- **日本語ファイル名:** ソースファイルは日本語名を使用する (例: `ボクセル世界.rs`)
  - `main.rs` のモジュール宣言には `#[path = "日本語名.rs"]` 属性を付ける
- **日本語識別子:** struct, enum, fn, const, フィールド名などすべて日本語を使う
  - 例: `struct チャンク`, `fn 丘陵地形生成()`, `pub 読込済み: HashMap<...>`
  - Rust 1.53 以降、非ASCII識別子は安定機能
- **コメント・仕様書:** すべて日本語
- **マジックナンバー禁止:** 数値は名前付き定数として定義する
- **新機能追加時:** `_doc/` に仕様を先に定義

## 会話履歴の保存ルール
- **目的:** 会話を別のAIに引き継ぐため、セッションの流れを記録する
- **保存場所:** `_doc/会話履歴/YYYY-MM-DD_セッション名.md`
- **記録内容:** ユーザーの入力要旨 → AIが考えたことの概要 → 行ったこと → 結果。細かなコードや思考過程は不要。流れの転換点があればその要因も記載
- **作成タイミング:** セッション開始時にユーザーが「この履歴の続き」と言わなかった場合、新規履歴ファイルを作成する。既存履歴の続きの場合は該当ファイルに追記する
- **コミット:** セッション終了時またはキリの良いタイミングで他の変更と一緒にコミットする

## 今後の改善候補
- チャンク管理の立方体/四面体の共通化 (トレイト抽出)
- Plugin トレイトの導入 (仕様書準拠)
- ディレクトリ構造の階層化 (`ドメイン/`, `描画/`, `UI/` 等)
